function fn(number) {
  return number * 2;
}
//アロー関数
const fnArrow = (number) => {
  return number * 2;
};

//無名関数
const fn = function (number) {
  return number * 2;
};

//無名関数の function を取り除き、 => を加えるとアロー関数になる
const fnArrow2 = (number) => {
  return number * 2;
};
